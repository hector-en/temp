--- workflow/authoritative-status.md
+++ workflow/authoritative-status.md
@@ -1,4 +1,4 @@
-# Configure HQ Authoritative Status This file is the human-facing source of truth for active development. Agents update it during planning, QA review, coding, and increment closeout so stakeholders can read one document instead of reconstructing state from chats, tests, or stash history.
+# Configure HQ Authoritative Status This file is the human-facing source of truth for active development. Agents update it during planning, QA review, coding, and increment closeout so stakeholders can read one document instead of reconstructing state from chats, tests, or stash history.
@@ -1,4 +1,4 @@
-## Current Snapshot - Current focus: `iscsi-tool-lab-vhdx-discovery` - Overall status: `active` - Last reviewed: `2026-04-08` - Active branch: `feat/iscsi` - Structured backing store: `workflow/stash-memory.yaml` ## Current Increment ### iscsi-tool-lab-vhdx-discovery - Status: `active` - Owner: `powershell-pair-coder` - Goal: discover safe Lab child and parent VHDX choices before any creation or iSCSI target publication work ## Hotfix Track ### bugfix - Status: `merged` - Head checkpoint: `3758619` - Base checkpoint: `daf202d` - Goal: harden HQ-local identity restore when `LocalAccounts` cmdlets cannot see or manage existing local SAM principals on the guest ### Current Progress - Active development lives under `app/` - Increment 3 is complete and tagged as `configure-hq-increment-3` - Increment 4 now has real code and tests for: - managed role ACL definitions and NTFS ACL orchestration helpers - required HQ principal definitions for mirrored users, ACL groups, and `HQ\\svc_lab` - guest-side principal state export and wrapper entrypoint via `-ExportSecurityPrincipalState` - create-or-detect handling for missing required HQ users and groups - required group membership handling for existing HQ users - optional identity restore during activation via `-RunIdentityRestore` - ACL application inside activation after the final workflow rows are built - saved-state cleanup planning for added group links and principals - explicit identity cleanup via `-RunIdentityCleanup`, including link removal, planned-removal review, and confirmed user and group deletion in one pass - comment-first cleanup so intent comments sit on the exact code and tests they describe - The Hyper-V host `P50` is not joined to `HQ`, so ACL and SMB policy must be authored in terms of HQ-local principals rather than `P50\...` identities - WSL2 access from `P50` is expected to arrive through an SMB session authenticated with HQ-local credentials - Latest increment-4 checkpoint is commit `daf202d` tagged as `configure-hq-increment-4` - The merged bugfix track on `dev` includes refreshed post-activation drive-letter discovery, stricter stale-ADSI cleanup planning, tolerant duplicate-create handling for hidden existing principals, and restore-time hidden-local-SAM verification - Guest reruns and the finished SMB closeout established a clean handoff into a separate iSCSI tool boundary - A new design-only iSCSI note now defines how an optional block publication mode would work for a writable child VHDX while keeping the parent VHDX read-only - A dedicated iSCSI test file now exists at `app/tests/configure_hq.iscsi.Tests.ps1` - The current red boundary is confirmed by a failing Pester run because `Get-HqLabVhdDiscoveryChoices` does not exist yet - The current discovery slice expects: - existing child VHDX candidates under `\\10.100.0.10\lab\virtual hdds frontends` - existing parent VHDX candidates under `\\10.100.0.10\lab\virtual hdds` - a default create-new option for both child and parent choice sets - This slice does not yet create folders, create VHDX files, or publish iSCSI targets - Historical SMB and increment-2 checkpoints remain available for lineage and rollback analysis ### QA Reading - Tags: `ready-for-green`, `hypothesis`, `risk-identified` - Commentary: the first iSCSI discovery test is in place and failing at the intended missing-function boundary.
-The finished SMB tool remains closed. The next safe move is the minimal green helper that only enumerates child and parent VHDX choices and returns create-new defaults.
-- Next steps: - implement `Get-HqLabVhdDiscoveryChoices` - keep the green slice at discovery only - add a dedicated execution switch after the helper boundary is stable - Quality risks: - real share enumeration may expose folder-layout or naming variations beyond the first discovery test - the Windows iSCSI target support boundary for publishing a differencing child disk is still unverified ### Blockers - no active blockers remain for the completed SMB closeout - the iSCSI publication path is intentionally split into a separate upcoming tool and still needs its own implementation plan ### Open Questions - Resolved: use `V:\VHDs\disks\sharedisk.vhdx` as the `ShareDrive` VHD path.
+## Current Snapshot - Current focus: `iscsi-tool-lab-vhdx-discovery` - Overall status: `active` - Last reviewed: `2026-04-10` - Active branch: `feat/iscsi` - Structured backing store: `workflow/stash-memory.yaml` ## Current Increment ### iscsi-tool-lab-vhdx-discovery - Status: `active` - Owner: `powershell-pair-coder` - Goal: discover safe Lab child and parent VHDX choices before any creation or iSCSI target publication work ## Hotfix Track ### bugfix - Status: `merged` - Head checkpoint: `3758619` - Base checkpoint: `daf202d` - Goal: harden HQ-local identity restore when `LocalAccounts` cmdlets cannot see or manage existing local SAM principals on the guest ### Current Progress - Active development lives under `app/` - Increment 3 is complete and tagged as `configure-hq-increment-3` - Increment 4 now has real code and tests for: - managed role ACL definitions and NTFS ACL orchestration helpers - required HQ principal definitions for mirrored users, ACL groups, and `HQ\\svc_lab` - guest-side principal state export and wrapper entrypoint via `-ExportSecurityPrincipalState` - create-or-detect handling for missing required HQ users and groups - required group membership handling for existing HQ users - optional identity restore during activation via `-RunIdentityRestore` - ACL application inside activation after the final workflow rows are built - saved-state cleanup planning for added group links and principals - explicit identity cleanup via `-RunIdentityCleanup`, including link removal, planned-removal review, and confirmed user and group deletion in one pass - comment-first cleanup so intent comments sit on the exact code and tests they describe - The Hyper-V host `P50` is not joined to `HQ`, so ACL and SMB policy must be authored in terms of HQ-local principals rather than `P50\...` identities - WSL2 access from `P50` is expected to arrive through an SMB session authenticated with HQ-local credentials - Latest increment-4 checkpoint is commit `daf202d` tagged as `configure-hq-increment-4` - The merged bugfix track on `dev` includes refreshed post-activation drive-letter discovery, stricter stale-ADSI cleanup planning, tolerant duplicate-create handling for hidden existing principals, and restore-time hidden-local-SAM verification - Guest reruns and the finished SMB closeout established a clean handoff into a separate iSCSI tool boundary - A new design-only iSCSI note now defines how an optional block publication mode would work for a writable child VHDX while keeping the parent VHDX read-only - A dedicated iSCSI test file now exists at `app/tests/configure_hq.iscsi.Tests.ps1` - `Get-HqLabVhdDiscoveryChoices` now exists in `app/configure_hq.ps1` and returns child and parent discovery payloads with `CreateNew` defaults plus `UseExisting` VHDX entries from the Lab shares - The helper still stays inside the intended green boundary: discovery only, with no folder creation, VHDX creation, or iSCSI target publication - `patches/iscsi-discovery-fix.patch` records a follow-up refinement to the helper's enumeration pipeline after the first implementation landed - Historical SMB and increment-2 checkpoints remain available for lineage and rollback analysis ### QA Reading - Tags: `needs-observation`, `risk-identified`, `hypothesis` - Commentary: the published code summary now shows the discovery helper implemented, but the workflow artifacts still describe the earlier missing-function red boundary.
+The immediate need is to reconcile repo memory with code reality and re-run the dedicated iSCSI Pester slice so the checkpoint is backed by fresh verification rather than inference from file contents alone.
+- Next steps: - run `Invoke-Pester app/tests/configure_hq.iscsi.Tests.ps1` against the current tree - update the workflow files to reflect the implemented helper and the latest observed test result - keep the next behavior slice limited to operator selection or a dedicated execution switch, not target publication - Quality risks: - real share enumeration may expose folder-layout or naming variations beyond the first mocked discovery test - the Windows iSCSI target support boundary for publishing a differencing child disk is still unverified - the latest helper refinement is present as a patch file, so the committed code and the saved checkpoint may still need explicit reconciliation ### Blockers - no active blockers remain for the discovery helper itself - the iSCSI publication path is intentionally split into a separate upcoming tool and still needs its own implementation plan ### Open Questions - Resolved: use `V:\VHDs\disks\sharedisk.vhdx` as the `ShareDrive` VHD path.
--- workflow/stash-memory.yaml
+++ workflow/stash-memory.yaml
@@ -1,2 +1,2 @@
-schema_version: 1 project: configure-hq human_status_file: "workflow/authoritative-status.md" global: current_focus: "iscsi-tool-lab-vhdx-discovery" overall_status: "active" last_reviewed: 2026-04-08" active_branch: "feat/iscsi" active_checkpoint: "5e6fb94" stakeholder_feedback: - "When the user says save here or equivalent, record the real progress state and create a stash before ending the session." - "Comment-first drift should be corrected before widening the next slice." - "Workflow artifacts should reflect live bugfix work before the next feature slice starts." - "AGENTS.md should bootstrap the repo workflow automatically for non-trivial work." resume_strategy: "time-series recomposition" resume_policy: - "Treat stash history as an experiment timeline, not just a restore stack." - "On resume, decide between single-stash resume and recomposed resume." - "Prefer the strongest next code state, even if it combines lessons from multiple stashes." - "Keep useful historical stashes until they are clearly superseded." qa_state_catalog: exploring: commentary: "Understanding is still forming." next_steps: "Inspect more code, tests, or real behavior." needs-criteria: commentary: "Acceptance criteria are not yet strong enough." next_steps: "Define measurable expected behavior before coding further." needs-observation: commentary: "Real system behavior has not been observed yet." next_steps: "Run the relevant command or inspect the environment." hypothesis: commentary: "The current approach depends on an unverified assumption." next_steps: "Create the cheapest validation step." risk-identified: commentary: "A concrete delivery or behavior risk is present." next_steps: "Mitigate or explicitly accept the risk." regression-risk: commentary: "Current changes may break working behavior." next_steps: "Add or strengthen tests around the affected path." test-gap: commentary: "Important behavior is not covered by tests." next_steps: "Add a failing test before more implementation." stakeholder-review: commentary: "The code direction needs stakeholder confirmation." next_steps: "Ask the narrowest question that resolves direction." ready-for-red: commentary: "The next behavior slice is clear enough to test." next_steps: "Write the next failing test." ready-for-green: commentary: "The failing test and implementation boundary are clear." next_steps: "Apply the minimal code change." ready-for-merge: commentary: "The increment appears coherent and reviewable." next_steps: "Do final verification and integrate." risk-accepted: commentary: "A known risk remains and is being carried intentionally." next_steps: "Record the risk and revisit later." increments:
+schema_version: 1 project: configure-hq human_status_file: "workflow/authoritative-status.md" global: current_focus: "iscsi-tool-lab-vhdx-discovery" overall_status: "active" last_reviewed: "2026-04-10" active_branch: "feat/iscsi" active_checkpoint: "5e6fb94" stakeholder_feedback: - "When the user says save here or equivalent, record the real progress state and create a stash before ending the session." - "Comment-first drift should be corrected before widening the next slice." - "Workflow artifacts should reflect live bugfix work before the next feature slice starts." - "AGENTS.md should bootstrap the repo workflow automatically for non-trivial work." resume_strategy: "time-series recomposition" resume_policy: - "Treat stash history as an experiment timeline, not just a restore stack." - "On resume, decide between single-stash resume and recomposed resume." - "Prefer the strongest next code state, even if it combines lessons from multiple stashes." - "Keep useful historical stashes until they are clearly superseded." qa_state_catalog: exploring: commentary: "Understanding is still forming." next_steps: "Inspect more code, tests, or real behavior." needs-criteria: commentary: "Acceptance criteria are not yet strong enough." next_steps: "Define measurable expected behavior before coding further." needs-observation: commentary: "Real system behavior has not been observed yet." next_steps: "Run the relevant command or inspect the environment." hypothesis: commentary: "The current approach depends on an unverified assumption." next_steps: "Create the cheapest validation step." risk-identified: commentary: "A concrete delivery or behavior risk is present." next_steps: "Mitigate or explicitly accept the risk." regression-risk: commentary: "Current changes may break working behavior." next_steps: "Add or strengthen tests around the affected path." test-gap: commentary: "Important behavior is not covered by tests." next_steps: "Add a failing test before more implementation." stakeholder-review: commentary: "The code direction needs stakeholder confirmation." next_steps: "Ask the narrowest question that resolves direction." ready-for-red: commentary: "The next behavior slice is clear enough to test." next_steps: "Write the next failing test." ready-for-green: commentary: "The failing test and implementation boundary are clear." next_steps: "Apply the minimal code change." ready-for-merge: commentary: "The increment appears coherent and reviewable." next_steps: "Do final verification and integrate." risk-accepted: commentary: "A known risk remains and is being carried intentionally." next_steps: "Record the risk and revisit later." increments:
@@ -1,2 +1,2 @@
-- id: "iscsi-tool-lab-vhdx-discovery" status: "active" summary: "The first red iSCSI slice adds a dedicated test for Lab child and parent VHDX discovery before any target publication work." owner: "powershell-pair-coder" stash_ref: "stash@{0}" qa_tags: ["ready-for-green", "hypothesis", "risk-identified"] qa_commentary: "The first iSCSI discovery test is in place and failing at the intended missing-function boundary. The new test fails because Get-HqLabVhdDiscoveryChoices does not exist yet, which is the intended green entrypoint." qa_next_steps: - "Implement the minimal Get-HqLabVhdDiscoveryChoices function." - "Return existing child and parent VHDX candidates plus create-new defaults." - "Keep creation and iSCSI publication out of this green slice." quality_risks: - "Real share enumeration may expose folder-layout differences beyond the first red test." - "Windows iSCSI target support for publishing the differencing child disk is still not verified." - "Interactive operator selection is still out of scope for the current slice." blockers: [] open_questions: - "Which Lab-backed child VHDX should become the safe publication candidate after discovery?" - "What naming rules should the create-new defaults use?" - "Should the standalone discovery switch land in the same slice or the next one?" hotfix_tracks:
+- id: "iscsi-tool-lab-vhdx-discovery" status: "active" summary: "The dedicated iSCSI discovery slice now has an implemented helper that enumerates existing Lab child and parent VHDX candidates and prepends create-new defaults without yet creating or publishing anything." owner: "powershell-pair-coder" stash_ref: "stash@{0}" qa_tags: ["needs-observation", "risk-identified", "hypothesis"] qa_commentary: "The published code summary now includes Get-HqLabVhdDiscoveryChoices in app/configure_hq.ps1, while the workflow memory still reflects the older red checkpoint. The helper appears to match the test intent, but this checkpoint still needs a fresh Pester run to replace inference with observed behavior." qa_next_steps: - "Run the dedicated iSCSI Pester file against the current tree." - "Record whether patches/iscsi-discovery-fix.patch has already been applied in the live branch or still needs to be folded in." - "Keep the next slice limited to discovery follow-through such as operator selection or a dedicated execution switch." quality_risks: - "Real share enumeration may expose folder-layout differences beyond the first mocked discovery test." - "Windows iSCSI target support for publishing the differencing child disk is still not verified." - "The checkpoint may lag the live branch because the repo summary also contains a follow-up patch for the helper." blockers: [] open_questions: - "Which Lab-backed child VHDX should become the safe publication candidate after discovery?" - "What naming rules should the create-new defaults use?" - "Should the standalone discovery switch land in the same slice or the next one?" hotfix_tracks:
@@ -1,2 +1,2 @@
-- timestamp: "2026-04-08T00:00:00+01:00" increment: "iscsi-tool-lab-vhdx-discovery" status: "wip" stash_ref: "stash@{0}" message: "red: completed for Get-HqLabVhdDiscoveryChoices" notes: "Latest red checkpoint.
-Test file app/tests/configure_hq.iscsi.Tests.ps1 exists and intentionally fails because the helper does not exist yet."
+- timestamp: "2026-04-10T00:00:00+01:00" increment: "iscsi-tool-lab-vhdx-discovery" status: "wip" stash_ref: "stash@{0}" message: "checkpoint: discovery helper implemented; verification still pending" notes: "Published code summary shows Get-HqLabVhdDiscoveryChoices implemented in app/configure_hq.ps1 and a follow-up helper refinement recorded in patches/iscsi-discovery-fix.patch. Workflow memory now needs a fresh observed Pester run to confirm whether the live branch already includes that patch and whether the dedicated iSCSI slice is green."
